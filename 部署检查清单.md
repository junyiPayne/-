# BS系统 - 部署检查清单

## ✅ 部署前检查

### 1. 代码准备
- [x] 所有源代码已提交到 Git
- [x] 环境变量模板文件已创建（.env.example）
- [x] 配置文件已更新（config.py, vue.config.js）
- [x] 健康检查接口已添加（/api/health）
- [x] 错误处理已优化（生产环境隐藏详细错误）
- [x] 安全配置已完善（CORS、密钥检查）

### 2. 服务器准备
- [ ] 服务器已准备好（Linux/macOS/Windows）
- [ ] Python 3.9+ 已安装
- [ ] Node.js 18+ 已安装
- [ ] Nginx 已安装（用于反向代理）
- [ ] 域名已配置（可选，建议）
- [ ] SSL 证书已准备（可选，推荐）

### 3. 环境变量配置
- [ ] 已从 .env.example 复制创建 .env 文件
- [ ] SECRET_KEY 已生成并配置（生产环境必须！）
- [ ] JWT_SECRET_KEY 已生成并配置（生产环境必须！）
- [ ] CORS_ORIGINS 已配置为生产域名
- [ ] 数据库配置已设置（SQLite 或 MySQL）
- [ ] AI API Key 已配置（如果使用 AI 功能）

### 4. 数据库准备
- [ ] 数据库已初始化（运行 init_database.py）
- [ ] 默认管理员密码已修改（默认：admin/admin123）
- [ ] 数据库备份策略已制定

### 5. 前端构建
- [ ] 前端依赖已安装（npm install）
- [ ] .env.production 已配置
- [ ] 生产版本已构建（npm run build）
- [ ] 构建产物已部署到 Web 服务器

### 6. 后端部署
- [ ] 后端依赖已安装（pip install -r requirements.txt）
- [ ] 虚拟环境已创建并激活
- [ ] 日志目录已创建（logs/）
- [ ] Gunicorn 已安装
- [ ] 后端服务已启动（使用 start.sh 或 Gunicorn）

### 7. Nginx 配置
- [ ] Nginx 配置文件已创建并配置
- [ ] 前端静态文件路径已配置
- [ ] API 代理已配置
- [ ] SSL 证书已配置（如果使用 HTTPS）
- [ ] Nginx 配置已测试（nginx -t）
- [ ] Nginx 已重启

### 8. 安全配置
- [ ] 默认密码已修改
- [ ] 防火墙已配置（只开放必要端口）
- [ ] HTTPS 已启用（推荐）
- [ ] 安全头已配置（X-Frame-Options 等）
- [ ] 文件上传大小限制已配置

### 9. 监控和日志
- [ ] 日志目录已创建
- [ ] 日志轮转已配置
- [ ] 健康检查接口可访问
- [ ] 监控工具已配置（可选）

### 10. 测试验证
- [ ] 健康检查接口测试通过（/api/health）
- [ ] 前端页面可正常访问
- [ ] 用户登录功能正常
- [ ] API 接口调用正常
- [ ] 文件上传功能正常
- [ ] 数据库操作正常

---

## 🔧 快速部署命令

### 后端部署
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# 编辑 .env 文件
python init_database.py
mkdir -p logs
./start.sh
```

### 前端部署
```bash
cd frontend
npm install
# 编辑 .env.production
npm run build
# 将 dist 目录内容部署到 Web 服务器
```

### 验证部署
```bash
# 检查后端健康状态
curl http://localhost:5001/api/health

# 检查前端（通过 Nginx）
curl http://yourdomain.com/api/health
```

---

## ⚠️ 重要提醒

1. **生产环境必须修改默认密钥**：
   ```bash
   python -c "import secrets; print(secrets.token_urlsafe(32))"
   ```

2. **立即修改默认管理员密码**：
   - 默认用户名：admin
   - 默认密码：admin123

3. **配置 CORS 允许的域名**：
   - 在 .env 中设置 CORS_ORIGINS

4. **定期备份数据库**：
   - SQLite: 复制 .db 文件
   - MySQL: 使用 mysqldump

5. **监控日志文件**：
   - backend/logs/app.log
   - backend/logs/error.log

---

## 📞 故障排除

如果遇到问题，请：
1. 查看日志文件
2. 检查环境变量配置
3. 验证服务是否运行
4. 检查端口是否被占用
5. 参考 deploy.md 文档

---

**部署完成后，请访问你的域名测试系统功能！**
