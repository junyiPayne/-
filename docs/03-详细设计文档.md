# 运动健康系统详细设计文档

## 1. 文档说明

### 1.1 编写目的
本文档详细描述运动健康系统各模块的实现方案，包括数据模型、接口设计、业务逻辑、前端组件等，为开发人员提供详细的实现指导。

### 1.2 文档结构
- 数据模型设计
- API接口详细设计
- 前端组件设计
- 业务逻辑流程
- 异常处理设计

## 2. 数据模型详细设计

### 2.1 用户模型 (User)

```python
class User(db.Model):
    """用户模型"""
    __tablename__ = 'users'
    
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True, nullable=False, index=True)
    email = db.Column(db.String(120), unique=True, nullable=False, index=True)
    password_hash = db.Column(db.String(255), nullable=False)
    real_name = db.Column(db.String(100))
    phone = db.Column(db.String(20))
    avatar = db.Column(db.String(255))
    role_id = db.Column(db.Integer, db.ForeignKey('roles.id'))
    is_active = db.Column(db.Boolean, default=True)
    last_login = db.Column(db.DateTime)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    # 关系
    role = db.relationship('Role', backref='users')
    profile = db.relationship('Profile', backref='user', uselist=False)
    daily_logs = db.relationship('DailyLog', backref='user', lazy='dynamic')
    
    def set_password(self, password):
        """设置密码"""
        self.password_hash = bcrypt.generate_password_hash(password).decode('utf-8')
    
    def check_password(self, password):
        """验证密码"""
        return bcrypt.check_password_hash(self.password_hash, password)
```

### 2.2 档案模型 (UserProfile)

```python
class UserProfile(db.Model):
    """用户生理档案模型"""
    __tablename__ = 'user_profiles'
    
    id = db.Column(db.Integer, primary_key=True)
    user_id = db.Column(db.Integer, db.ForeignKey('users.id'), unique=True, nullable=False, index=True)
    
    # 基础数据
    gender = db.Column(db.String(10))  # 性别: male/female
    birthday = db.Column(db.Date)  # 出生日期
    age = db.Column(db.Integer)  # 年龄
    height_cm = db.Column(db.Float)  # 身高(cm)
    weight_kg = db.Column(db.Float)  # 体重(kg)
    waist_cm = db.Column(db.Float)  # 腰围(cm)
    hip_cm = db.Column(db.Float)  # 臀围(cm)
    body_fat_percent = db.Column(db.Float)  # 体脂百分比
    
    # 计算指标
    bmi = db.Column(db.Float)  # BMI
    bmr = db.Column(db.Float)  # 基础代谢率(kcal/天)
    whr = db.Column(db.Float)  # 腰臀比
    whtr = db.Column(db.Float)  # 腰高比
    
    # 评估结果
    weight_category = db.Column(db.String(50))  # 体重等级
    body_fat_category = db.Column(db.String(50))  # 体脂等级
    
    created_at = db.Column(db.DateTime, default=datetime.utcnow, nullable=False)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, nullable=False)
    
    # 关系
    user = db.relationship('User', backref='profile', uselist=False)
```

### 2.3 日志模型 (DailyLog)

```python
class DailyLog(db.Model):
    """每日记录模型"""
    __tablename__ = 'daily_logs'
    
    id = db.Column(db.Integer, primary_key=True)
    user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)
    log_date = db.Column(db.Date, nullable=False, index=True)
    
    # 饮食记录
    calorie_intake = db.Column(db.Float, default=0)  # 每日摄入热量(kcal)
    carb_percent = db.Column(db.Float)  # 碳水百分比
    protein_percent = db.Column(db.Float)  # 蛋白质百分比
    fat_percent = db.Column(db.Float)  # 脂肪百分比
    fiber_grams = db.Column(db.Float)  # 膳食纤维(克)
    alcohol_grams = db.Column(db.Float, default=0)  # 酒精(克)
    food_images = db.Column(db.Text)  # 食物图片URL（JSON格式存储）
    food_description = db.Column(db.Text)  # 食物描述
    
    # 运动记录
    exercise_type = db.Column(db.String(100))  # 运动类型
    exercise_duration = db.Column(db.Integer)  # 运动时长(分钟)
    exercise_intensity = db.Column(db.String(50))  # 运动强度
    exercise_frequency = db.Column(db.Integer)  # 运动频率(次/周)
    calorie_expenditure = db.Column(db.Float, default=0)  # 运动消耗热量(kcal)
    
    # 身体指标记录（每日测量）
    daily_weight = db.Column(db.Float)  # 当日体重(kg)
    daily_waist = db.Column(db.Float)  # 当日腰围(cm)
    daily_hip = db.Column(db.Float)  # 当日臀围(cm)
    
    # 计算指标
    net_calorie = db.Column(db.Float)  # 净热量(摄入-消耗)
    predicted_weight_change = db.Column(db.Float)  # 预测体重变化(kg)
    
    # AI分析
    ai_risk_assessment = db.Column(db.Text)  # AI健康风险评估
    ai_suggestions = db.Column(db.Text)  # AI建议（JSON格式）
    
    created_at = db.Column(db.DateTime, default=datetime.utcnow, nullable=False)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, nullable=False)
    
    # 关系
    user = db.relationship('User', backref='daily_logs', lazy=True)
    
    __table_args__ = (db.UniqueConstraint('user_id', 'log_date', name='_user_date_uc'),)
```

### 2.4 角色模型 (Role)

```python
class Role(db.Model):
    """角色模型"""
    __tablename__ = 'roles'
    
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(50), unique=True, nullable=False)
    code = db.Column(db.String(50), unique=True, nullable=False, index=True)
    description = db.Column(db.Text)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    # 多对多关系
    permissions = db.relationship('Permission', secondary='role_permissions', backref='roles')
    
    def to_dict(self):
        """转换为字典"""
        return {
            'id': self.id,
            'name': self.name,
            'code': self.code,
            'description': self.description,
            'permissions': [p.to_dict() for p in self.permissions],
            'created_at': self.created_at.isoformat()
        }
```

### 2.3 权限模型 (Permission)

```python
class Permission(db.Model):
    """权限模型"""
    __tablename__ = 'permissions'
    
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100), nullable=False)
    code = db.Column(db.String(100), unique=True, nullable=False, index=True)
    resource = db.Column(db.String(50))  # 资源类型: user, role, business等
    action = db.Column(db.String(50))    # 操作: create, read, update, delete
    description = db.Column(db.Text)
    
    def to_dict(self):
        """转换为字典"""
        return {
            'id': self.id,
            'name': self.name,
            'code': self.code,
            'resource': self.resource,
            'action': self.action,
            'description': self.description
        }
```

### 2.4 报告模型 (Report)

```python
class Report(db.Model):
    """学生报告模型 - 每个用户只有一个报告"""
    __tablename__ = 'reports'
    
    id = db.Column(db.Integer, primary_key=True)
    user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, unique=True)
    title = db.Column(db.String(200), nullable=False)
    file_path = db.Column(db.String(255), nullable=True)  # 保留字段，兼容旧数据
    pdf_content = db.Column(db.LargeBinary, nullable=True)  # PDF二进制内容
    data_hash = db.Column(db.String(64), nullable=True)  # 报告数据内容的哈希值
    status = db.Column(db.String(20), default='submitted')  # submitted, reviewed
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    # 关系
    user = db.relationship('User', backref=db.backref('report', uselist=False, lazy=True))
    history = db.relationship('ReportHistory', backref='report', lazy=True, 
                             order_by='ReportHistory.created_at.desc()')
```

### 2.5 报告历史模型 (ReportHistory)

```python
class ReportHistory(db.Model):
    """报告修改日志"""
    __tablename__ = 'report_history'
    
    id = db.Column(db.Integer, primary_key=True)
    report_id = db.Column(db.Integer, db.ForeignKey('reports.id'), nullable=False)
    modified_by_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False)
    action = db.Column(db.String(50), nullable=False)  # 'created', 'updated'
    description = db.Column(db.String(500))  # 修改描述
    created_at = db.Column(db.DateTime, default=datetime.utcnow, nullable=False)
    
    # 关系
    modified_by = db.relationship('User', foreign_keys=[modified_by_id], lazy=True)
```

### 2.6 业务数据模型 (BusinessData)

```python
class BusinessData(db.Model):
    """业务数据模型"""
    __tablename__ = 'business_data'
    
    id = db.Column(db.Integer, primary_key=True)
    title = db.Column(db.String(200), nullable=False)
    content = db.Column(db.Text)
    category = db.Column(db.String(50), index=True)
    status = db.Column(db.String(20), default='active', index=True)
    priority = db.Column(db.Integer, default=0)
    creator_id = db.Column(db.Integer, db.ForeignKey('users.id'))
    created_at = db.Column(db.DateTime, default=datetime.utcnow, index=True)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    # 关系
    creator = db.relationship('User', backref='business_data')
    
    def to_dict(self):
        """转换为字典"""
        return {
            'id': self.id,
            'title': self.title,
            'content': self.content,
            'category': self.category,
            'status': self.status,
            'priority': self.priority,
            'creator_id': self.creator_id,
            'creator_name': self.creator.username if self.creator else None,
            'created_at': self.created_at.isoformat(),
            'updated_at': self.updated_at.isoformat()
        }
```

## 3. API接口详细设计

### 3.1 认证接口

#### 3.1.1 用户注册

**接口**: `POST /api/auth/register`

**请求参数**:
```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "password123",
  "real_name": "测试用户"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "user_id": 1,
    "username": "testuser"
  }
}
```

**实现逻辑**:
1. 验证请求参数
2. 检查用户名和邮箱是否已存在
3. 创建用户记录
4. 设置密码哈希
5. 分配默认角色
6. 返回用户信息

#### 3.1.2 用户登录

**接口**: `POST /api/auth/login`

**请求参数**:
```json
{
  "username": "testuser",
  "password": "password123"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "refresh_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "expires_in": 86400,
    "user": {
      "id": 1,
      "username": "testuser",
      "email": "test@example.com",
      "role": "admin"
    }
  }
}
```

**实现逻辑**:
1. 验证用户名和密码
2. 检查用户是否激活
3. 生成JWT Token
4. 更新最后登录时间
5. 返回Token和用户信息

### 3.2 用户管理接口

#### 3.2.1 获取用户列表

**接口**: `GET /api/users`

**查询参数**:
- page: 页码 (默认: 1)
- per_page: 每页数量 (默认: 20)
- search: 搜索关键词
- role_id: 角色ID过滤

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "username": "testuser",
        "email": "test@example.com",
        "role_name": "管理员"
      }
    ],
    "total": 100,
    "page": 1,
    "per_page": 20,
    "pages": 5
  }
}
```

**实现逻辑**:
1. 验证用户权限
2. 构建查询条件
3. 分页查询用户
4. 序列化用户数据
5. 返回分页结果

### 3.3 业务数据接口

#### 3.3.1 创建业务数据

**接口**: `POST /api/business/data`

**请求参数**:
```json
{
  "title": "业务标题",
  "content": "业务内容",
  "category": "category1",
  "priority": 1
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "title": "业务标题",
    "created_at": "2024-01-01T00:00:00Z"
  }
}
```

**实现逻辑**:
1. 验证请求参数
2. 获取当前用户
3. 创建业务数据记录
4. 保存到数据库
5. 返回创建结果

### 3.4 用户档案接口

#### 3.4.1 获取用户档案

**接口**: `GET /api/profile`

**请求头**: `Authorization: Bearer <token>`

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "user_id": 1,
    "gender": "male",
    "age": 25,
    "height_cm": 175.0,
    "weight_kg": 70.0,
    "waist_cm": 80.0,
    "hip_cm": 95.0,
    "body_fat_percent": 15.0,
    "bmi": 22.86,
    "bmr": 1750.5,
    "whr": 0.84,
    "whtr": 0.46,
    "weight_category": "正常",
    "body_fat_category": "正常"
  }
}
```

#### 3.4.2 创建/更新用户档案

**接口**: `POST /api/profile` 或 `PUT /api/profile`

**请求参数**:
```json
{
  "gender": "male",
  "birthday": "1998-01-01",
  "age": 25,
  "height_cm": 175.0,
  "weight_kg": 70.0,
  "waist_cm": 80.0,
  "hip_cm": 95.0,
  "body_fat_percent": 15.0
}
```

### 3.5 每日日志接口

#### 3.5.1 获取日志列表

**接口**: `GET /api/daily-log`

**查询参数**:
- `start_date`: 开始日期（可选）
- `end_date`: 结束日期（可选）
- `page`: 页码（默认1）
- `per_page`: 每页数量（默认20）

#### 3.5.2 创建/更新日志

**接口**: `POST /api/daily-log`

**请求参数**:
```json
{
  "log_date": "2024-01-01",
  "calorie_intake": 2000,
  "carb_percent": 50,
  "protein_percent": 25,
  "fat_percent": 25,
  "fiber_grams": 25,
  "alcohol_grams": 0,
  "exercise_type": "跑步",
  "exercise_duration": 30,
  "exercise_intensity": "中等",
  "calorie_expenditure": 300,
  "daily_weight": 70.0,
  "daily_waist": 80.0,
  "daily_hip": 95.0
}
```

#### 3.5.3 获取统计数据

**接口**: `GET /api/daily-log/statistics`

**查询参数**:
- `weeks`: 统计周数（4/8/12，默认4）

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "avg_intake": 2000,
    "avg_expenditure": 300,
    "avg_net_calorie": 1700,
    "predicted_weight_change": -0.5,
    "total_days": 28
  }
}
```

### 3.6 AI服务接口

#### 3.6.1 获取AI健康评估

**接口**: `POST /api/ai/health-assessment`

**请求参数**:
```json
{
  "weeks": 4
}
```

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "risk_assessment": "您的BMI处于正常范围...",
    "suggestions": [
      "建议增加有氧运动...",
      "控制碳水摄入...",
      "保持规律作息..."
    ],
    "prediction": "预计4周后体重将下降0.5kg..."
  }
}
```

#### 3.6.2 获取AI预测

**接口**: `POST /api/ai/prediction`

**请求参数**:
```json
{
  "calorie_intake": 2000,
  "calorie_expenditure": 2200,
  "weeks": 4,
  "carb_percent": 50,
  "protein_percent": 20,
  "fat_percent": 30
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "预测完成",
  "data": {
    "assessment": {
      "weight": "-1.5 kg",
      "fat": "-0.8 %",
      "risks": ["风险1", "风险2"],
      "suggestions": ["建议1", "建议2"]
    },
    "prediction_period": "4周",
    "current_weight": 70.5,
    "predicted_weight_change": "-1.5 kg"
  }
}
```

**实现逻辑**:
1. 验证用户档案是否存在
2. 接收前端传入的摄入和消耗数据
3. 调用AI服务生成评估
4. 返回预测结果

### 3.7 报告管理接口

#### 3.7.1 预览报告

**接口**: `GET /api/report/preview`

**请求头**: `Authorization: Bearer <token>`

**说明**: 根据当前用户档案和日志数据生成PDF报告预览

**响应**: 返回PDF二进制流，Content-Type: `application/pdf`

#### 3.7.2 提交报告

**接口**: `POST /api/report/submit`

**请求头**: `Authorization: Bearer <token>`

**请求参数**: 无（自动使用当前用户数据）

**响应示例**:
```json
{
  "code": 200,
  "message": "报告提交成功",
  "data": {
    "report_id": 1,
    "title": "健康报告_2024-01-01",
    "status": "submitted"
  }
}
```

**业务逻辑**:
1. 根据当前用户档案和日志数据计算数据哈希值
2. 检查是否已存在报告
3. 如果存在，比较新旧哈希值
4. 如果哈希值相同，返回400错误："当前报告已经是最新，请修改报告内容再提交"
5. 如果哈希值不同或不存在报告，生成新PDF并保存
6. 记录报告历史（created或updated）

#### 3.7.3 获取报告列表

**接口**: `GET /api/report/list`

**请求头**: `Authorization: Bearer <token>`

**查询参数**:
- `include_history`: 是否包含历史记录（true/false，默认false）

**权限控制**:
- 管理员：可查看所有报告
- 教师：可查看学生的报告
- 学生：只能查看自己的报告

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "user_id": 1,
      "student_name": "张三",
      "title": "健康报告_2024-01-01",
      "status": "submitted",
      "created_at": "2024-01-01T10:00:00",
      "updated_at": "2024-01-01T10:00:00",
      "history": [
        {
          "id": 1,
          "action": "created",
          "description": "创建报告",
          "modified_by": "张三",
          "created_at": "2024-01-01T10:00:00"
        }
      ]
    }
  ]
}
```

#### 3.7.4 查看报告详情

**接口**: `GET /api/report/view/<report_id>`

**请求头**: `Authorization: Bearer <token>`

**权限控制**: 同报告列表

**响应**: 返回PDF二进制流，Content-Type: `application/pdf`

## 4. 前端组件详细设计

### 4.1 用户登录组件

**组件路径**: `frontend/src/views/auth/Login.vue`

**功能**:
- 用户登录表单
- 表单验证
- 登录状态管理
- 错误提示

**代码结构**:
```vue
<template>
  <div class="login-container">
    <el-form :model="loginForm" :rules="rules" ref="loginFormRef">
      <el-form-item prop="username">
        <el-input v-model="loginForm.username" placeholder="用户名" />
      </el-form-item>
      <el-form-item prop="password">
        <el-input type="password" v-model="loginForm.password" placeholder="密码" />
      </el-form-item>
      <el-form-item>
        <el-button type="primary" @click="handleLogin">登录</el-button>
      </el-form-item>
    </el-form>
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue'
import { useRouter } from 'vue-router'
import { useAuthStore } from '@/stores/auth'
import { ElMessage } from 'element-plus'

const router = useRouter()
const authStore = useAuthStore()

const loginForm = reactive({
  username: '',
  password: ''
})

const rules = {
  username: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
  password: [{ required: true, message: '请输入密码', trigger: 'blur' }]
}

const handleLogin = async () => {
  try {
    await authStore.login(loginForm)
    ElMessage.success('登录成功')
    router.push('/dashboard')
  } catch (error) {
    ElMessage.error(error.message || '登录失败')
  }
}
</script>
```

### 4.2 用户列表组件

**组件路径**: `frontend/src/views/users/UserList.vue`

**功能**:
- 用户列表展示
- 搜索和筛选
- 分页功能
- 用户操作(编辑、删除)

**主要方法**:
- `loadUsers()`: 加载用户列表
- `handleSearch()`: 搜索处理
- `handleEdit()`: 编辑用户
- `handleDelete()`: 删除用户

### 4.3 业务数据管理组件

**组件路径**: `frontend/src/views/business/DataManage.vue`

**功能**:
- 业务数据CRUD操作
- 数据表格展示
- 数据筛选和排序
- 数据导出

## 5. 业务逻辑流程

### 5.1 用户注册流程

```
开始
  ↓
填写注册信息
  ↓
前端验证
  ↓
发送注册请求
  ↓
后端验证参数
  ↓
检查用户名/邮箱是否存在
  ↓
[存在] → 返回错误信息 → 结束
  ↓ [不存在]
创建用户记录
  ↓
设置密码哈希
  ↓
分配默认角色
  ↓
保存到数据库
  ↓
返回成功信息
  ↓
结束
```

### 5.2 用户登录流程

```
开始
  ↓
输入用户名密码
  ↓
前端验证
  ↓
发送登录请求
  ↓
后端验证用户名密码
  ↓
[验证失败] → 返回错误 → 结束
  ↓ [验证成功]
检查用户状态
  ↓
[未激活] → 返回错误 → 结束
  ↓ [已激活]
生成JWT Token
  ↓
更新最后登录时间
  ↓
返回Token和用户信息
  ↓
前端存储Token
  ↓
跳转到主页
  ↓
结束
```

### 5.3 权限验证流程

```
请求到达
  ↓
提取Token
  ↓
验证Token有效性
  ↓
[无效] → 返回401错误 → 结束
  ↓ [有效]
获取用户信息
  ↓
获取用户权限
  ↓
检查请求权限
  ↓
[无权限] → 返回403错误 → 结束
  ↓ [有权限]
执行请求处理
  ↓
返回结果
  ↓
结束
```

## 6. 异常处理设计

### 6.1 后端异常处理

**异常类定义**:
```python
class APIException(Exception):
    """API异常基类"""
    def __init__(self, code, message, data=None):
        self.code = code
        self.message = message
        self.data = data

class ValidationError(APIException):
    """参数验证错误"""
    def __init__(self, message, data=None):
        super().__init__(400, message, data)

class AuthenticationError(APIException):
    """认证错误"""
    def __init__(self, message="认证失败"):
        super().__init__(401, message)

class PermissionDeniedError(APIException):
    """权限不足"""
    def __init__(self, message="权限不足"):
        super().__init__(403, message)

class NotFoundError(APIException):
    """资源不存在"""
    def __init__(self, message="资源不存在"):
        super().__init__(404, message)
```

**全局异常处理**:
```python
@app.errorhandler(APIException)
def handle_api_exception(e):
    return jsonify({
        'code': e.code,
        'message': e.message,
        'data': e.data,
        'timestamp': datetime.utcnow().isoformat()
    }), e.code

@app.errorhandler(Exception)
def handle_exception(e):
    logger.error(f"Unhandled exception: {str(e)}", exc_info=True)
    return jsonify({
        'code': 500,
        'message': '服务器内部错误',
        'timestamp': datetime.utcnow().isoformat()
    }), 500
```

### 6.2 前端异常处理

**HTTP拦截器**:
```javascript
// axios拦截器配置
axios.interceptors.response.use(
  response => response,
  error => {
    if (error.response) {
      const { code, message } = error.response.data
      switch (code) {
        case 401:
          // 清除token，跳转登录
          authStore.logout()
          router.push('/login')
          break
        case 403:
          ElMessage.error('权限不足')
          break
        case 404:
          ElMessage.error('资源不存在')
          break
        case 500:
          ElMessage.error('服务器错误')
          break
        default:
          ElMessage.error(message || '请求失败')
      }
    }
    return Promise.reject(error)
  }
)
```

## 7. 数据验证设计

### 7.1 后端验证

**使用Marshmallow Schema**:
```python
from marshmallow import Schema, fields, validate

class UserRegisterSchema(Schema):
    username = fields.Str(required=True, validate=validate.Length(min=3, max=80))
    email = fields.Email(required=True)
    password = fields.Str(required=True, validate=validate.Length(min=6, max=100))
    real_name = fields.Str(validate=validate.Length(max=100))

class UserLoginSchema(Schema):
    username = fields.Str(required=True)
    password = fields.Str(required=True)
```

### 7.2 前端验证

**使用Element Plus表单验证**:
```javascript
const rules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 3, max: 80, message: '长度在3到80个字符', trigger: 'blur' }
  ],
  email: [
    { required: true, message: '请输入邮箱', trigger: 'blur' },
    { type: 'email', message: '请输入正确的邮箱格式', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, max: 100, message: '密码长度在6到100个字符', trigger: 'blur' }
  ]
}
```

## 8. 安全设计详细说明

### 8.1 JWT Token设计

**Token结构**:
```json
{
  "header": {
    "alg": "HS256",
    "typ": "JWT"
  },
  "payload": {
    "user_id": 1,
    "username": "testuser",
    "role": "admin",
    "exp": 1234567890,
    "iat": 1234567890
  }
}
```

**Token刷新机制**:
- Access Token: 有效期24小时
- Refresh Token: 有效期7天
- 使用Refresh Token获取新的Access Token

### 8.2 密码加密

**使用bcrypt**:
```python
import bcrypt

# 加密
password_hash = bcrypt.generate_password_hash(password).decode('utf-8')

# 验证
is_valid = bcrypt.check_password_hash(password_hash, password)
```

### 8.3 SQL注入防护

**使用ORM参数化查询**:
```python
# 正确的方式
user = User.query.filter_by(username=username).first()

# 错误的方式(容易被注入)
user = User.query.filter(f"username = '{username}'").first()
```

## 9. 性能优化设计

### 9.1 数据库查询优化

**索引设计**:
```sql
-- 用户表索引
CREATE INDEX idx_users_username ON users(username);
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_role_id ON users(role_id);

-- 业务数据表索引
CREATE INDEX idx_business_data_category ON business_data(category);
CREATE INDEX idx_business_data_status ON business_data(status);
CREATE INDEX idx_business_data_created_at ON business_data(created_at);
```

**查询优化**:
```python
# 使用eager loading减少查询次数
users = User.query.options(
    db.joinedload(User.role)
).all()

# 使用分页
users = User.query.paginate(page=1, per_page=20)
```

### 9.2 前端性能优化

**代码分割**:
```javascript
// 路由懒加载
const Login = () => import('@/views/auth/Login.vue')
const Dashboard = () => import('@/views/Dashboard.vue')
```

**防抖和节流**:
```javascript
import { debounce } from 'lodash-es'

const handleSearch = debounce((keyword) => {
  // 搜索逻辑
}, 300)
```

## 10. 日志设计

### 10.1 后端日志

**日志级别**:
- DEBUG: 调试信息
- INFO: 一般信息
- WARNING: 警告信息
- ERROR: 错误信息
- CRITICAL: 严重错误

**日志记录**:
```python
import logging

logger = logging.getLogger(__name__)

logger.info(f"User {user_id} logged in")
logger.error(f"Database error: {str(e)}", exc_info=True)
```

### 10.2 前端日志

**使用console或日志服务**:
```javascript
// 开发环境
if (process.env.NODE_ENV === 'development') {
  console.log('Debug info:', data)
}

// 生产环境
// 发送到日志服务
logService.error('Error occurred', error)
```

## 11. 测试设计

### 11.1 单元测试

**后端测试示例**:
```python
import pytest
from app.models import User

def test_user_creation():
    user = User(username='test', email='test@example.com')
    user.set_password('password123')
    assert user.check_password('password123')
    assert not user.check_password('wrongpassword')
```

**前端测试示例**:
```javascript
import { mount } from '@vue/test-utils'
import Login from '@/views/auth/Login.vue'

describe('Login.vue', () => {
  it('should render login form', () => {
    const wrapper = mount(Login)
    expect(wrapper.find('form').exists()).toBe(true)
  })
})
```

### 11.2 集成测试

**API测试示例**:
```python
def test_user_registration(client):
    response = client.post('/api/auth/register', json={
        'username': 'newuser',
        'email': 'newuser@example.com',
        'password': 'password123'
    })
    assert response.status_code == 200
    assert 'user_id' in response.json['data']
```

## 12. 部署设计

### 12.1 环境变量配置

**生产环境配置**:
```env
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=production-secret-key
DB_HOST=prod-db-host
DB_PASSWORD=prod-password
```

### 12.2 Docker部署

**Dockerfile示例**:
```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:5000", "app:app"]
```

## 13. 附录

### 13.1 数据字典

见数据库设计文档

### 13.2 API接口文档

见API接口文档

### 13.3 错误码列表

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

