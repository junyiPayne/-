Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Migrate==4.0.5
Flask-JWT-Extended==4.5.3
Flask-CORS==4.0.0
Flask-Marshmallow==0.15.0
marshmallow-sqlalchemy==0.29.0
python-dotenv==1.0.0
bcrypt==4.1.1
PyMySQL==1.1.0
cryptography==41.0.7
Werkzeug==2.3.7
gunicorn==21.2.0
requests==2.31.0
dashscope==1.17.0

Pillow==10.0.0
fpdf2==2.7.6
matplotlib==3.8.2

# ============================================
# GPU 支持（可选依赖，仅在使用 GPU 功能时需要）
# ============================================
# 
# Windows 本地部署（需要 GPU）：
#   取消下面的注释，然后安装 PyTorch CUDA 版本：
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu117
#
# Docker 部署（GPU 版本）：
#   PyTorch 已在 Dockerfile.gpu 的基础镜像中包含，无需单独安装
#
# 不使用 GPU 功能：
#   可以忽略以下依赖，代码会自动使用 CPU
#
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0
