# 🚀 快速启动指南

本文档提供最简单的一键启动方式，支持本地开发模式和Docker开发模式。

---

## 🎯 一键启动（推荐）

### 方式一：本地开发模式

**macOS/Linux：**
在项目根目录执行：
```bash
./start-local.sh
```

**Windows：**
在项目根目录执行：
```cmd
start-local.bat
```

**就这么简单！** 脚本会自动：
- ✅ 检查Python、Node.js环境
- ✅ 创建虚拟环境并安装依赖
- ✅ 初始化数据库
- ✅ 启动后端服务（端口5001）
- ✅ 启动前端服务（端口8080）

**访问地址：**
- 前端：http://localhost:8080
- 后端API：http://localhost:5001/api/health

**默认账户：**
- 用户名：`admin`
- 密码：`admin123`

**停止服务：**
- macOS/Linux: `./stop-local.sh`
- Windows: `stop-local.bat` 或关闭命令行窗口

---

### 方式二：Docker开发模式

在项目根目录执行：

```bash
./start-docker.sh
```

**就这么简单！** 脚本会自动：
- ✅ 检查Docker环境
- ✅ 启动所有容器（默认不重建镜像）
- ✅ 检查服务健康状态

如需强制重新构建镜像（前端/后端依赖有变化时）：

```bash
./start-docker.sh --rebuild
```

**访问地址：**
- 前端：http://localhost（通过Nginx）
- 后端API：http://localhost:5001/api/health

**默认账户：**
- 用户名：`admin`
- 密码：`admin123`

**停止服务：**
```bash
docker-compose down
```

---

## 🔧 配置说明

### AI功能配置（可选）

如需使用AI健康分析功能，在项目根目录创建 `.env` 文件：

```bash
# DeepSeek API
DEEPSEEK_API_KEY=your-api-key-here
AI_PROVIDER=deepseek

# 或使用通义千问
QWEN_API_KEY=your-api-key-here
AI_PROVIDER=qwen
```

**配置后重启服务：**
- 本地模式：`./stop-local.sh && ./start-local.sh`
- Docker模式：`docker-compose restart backend`

---

## 🔍 故障排查

### 0. 后端显示unhealthy

如果启动后后端显示unhealthy，运行诊断脚本：

```bash
./check-health.sh
```

**常见原因和解决方法：**

1. **数据库连接失败**
   ```bash
   # 检查数据库文件是否存在
   ls -la backend/instance/bs_system.db
   
   # 如果不存在，重新初始化
   cd backend
   source venv/bin/activate
   python init_database.py
   ```

2. **数据库文件权限问题**
   ```bash
   # 修复权限
   chmod 664 backend/instance/bs_system.db
   ```

3. **依赖包缺失**
   ```bash
   cd backend
   source venv/bin/activate
   pip install -r requirements.txt
   ```

4. **查看详细错误信息**
   ```bash
   # 查看后端日志的最后50行
   tail -n 50 backend.log
   
   # 实时查看日志
   tail -f backend.log
   
   # 手动测试健康接口（查看详细错误）
   curl http://localhost:5001/api/health | python3 -m json.tool
   ```

5. **如果数据库文件损坏**
   ```bash
   # 备份现有数据库
   cd backend
   cp instance/bs_system.db instance/bs_system.db.backup
   
   # 删除损坏的数据库
   rm instance/bs_system.db
   
   # 重新初始化
   source venv/bin/activate
   python init_database.py
   ```

**常见错误信息解读：**

- `unhealthy: no such table` - 数据库表不存在，需要重新初始化
- `unhealthy: database is locked` - 数据库被锁定，可能是另一个进程在使用
- `unhealthy: unable to open database file` - 数据库文件路径错误或权限问题
- `ModuleNotFoundError` - 缺少Python依赖包，运行 `pip install -r requirements.txt`

**快速修复（推荐）：**
```bash
# 运行自动修复脚本
./fix-database.sh
```

---

### 1. 端口被占用

**本地模式：**
```bash
# macOS/Linux - 释放端口5001
lsof -ti:5001 | xargs kill -9

# macOS/Linux - 释放端口8080
lsof -ti:8080 | xargs kill -9

# Windows - 查找占用5001端口的进程
netstat -ano | findstr :5001
# 然后杀死进程（替换<PID>为实际PID）
taskkill /PID <PID> /F
```

**Docker模式：**
```bash
# 释放端口80
lsof -ti:80 | xargs kill -9

# 或修改docker-compose.yml中的端口映射
```

---

### 2. 后端启动失败

**检查后端日志：**
```bash
# 本地模式
tail -f backend.log

# Docker模式
docker-compose logs -f backend
```

**常见问题：**

- **数据库错误**：删除 `backend/instance/bs_system.db` 后重新运行 `./start-local.sh`
- **依赖缺失**：进入 `backend` 目录，运行 `pip install -r requirements.txt`
- **Python版本不对**：确保使用Python 3.9+

**手动初始化数据库：**
```bash
cd backend
source venv/bin/activate  # macOS/Linux
python init_database.py
```

---

### 3. 前端启动失败

**检查前端日志：**
```bash
# 本地模式
tail -f frontend.log

# Docker模式
docker-compose logs -f nginx
```

**常见问题：**

- **npm install 失败**：使用国内镜像
  ```bash
  npm config set registry https://registry.npmmirror.com
  npm install
  ```
- **node_modules损坏**：删除后重新安装
  ```bash
  cd frontend
  rm -rf node_modules package-lock.json
  npm install
  ```
- **端口8080被占用**：参考上面的端口释放方法

---

### 4. Docker相关问题

**Docker未启动：**
```bash
# macOS - 打开Docker Desktop应用
# Linux - 启动Docker服务
sudo systemctl start docker
```

**容器启动失败：**
```bash
# 查看所有容器状态
docker-compose ps

# 查看详细日志
docker-compose logs

# 重新构建
docker-compose up -d --build
```

**前端代码更新后不生效（Docker模式）：**
```bash
cd frontend
npm run build
cd ..
docker-compose restart nginx
# 浏览器按 Cmd/Ctrl+Shift+R 硬刷新
```

---

### 5. 数据库问题

**SQLite数据库损坏：**
```bash
# 备份并删除数据库文件
cd backend
mv instance/bs_system.db instance/bs_system.db.backup
python init_database.py
```

**切换到MySQL（Docker模式）：**
在 `.env` 文件中配置：
```bash
DB_TYPE=mysql
DB_HOST=db
DB_PORT=3306
DB_USER=bs_user
DB_PASSWORD=your_password
DB_NAME=bs_system
```

---

### 6. 网络连接问题

**前端无法连接后端：**

- **本地模式**：检查 `frontend/vue.config.js` 中的代理配置
- **Docker模式**：检查 `docker-compose.yml` 和 `nginx.conf` 配置

**API请求返回401错误：**
- 清除浏览器缓存和localStorage
- 重新登录

**API请求超时：**
- 检查后端是否正常运行
- 检查防火墙设置
- 增加超时时间（在 `frontend/src/api/request.js` 中）

---

### 7. 权限问题（macOS/Linux）

**脚本无法执行：**
```bash
chmod +x start-local.sh start-docker.sh stop-local.sh
```

**虚拟环境权限错误：**
```bash
cd backend
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

## 📋 手动启动（高级用户）

如果一键启动脚本无法使用，可以手动启动：

### 本地开发模式手动启动

**终端1 - 启动后端：**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
pip install -r requirements.txt
python init_database.py
python run.py
```

**终端2 - 启动前端：**
```bash
cd frontend
npm install
npm run serve
```

### Docker模式手动启动

```bash
docker-compose up -d --build
docker-compose logs -f
```

---

## 🛠️ 工具脚本

项目在根目录提供了一组启动 / 诊断 / 修复脚本，方便开发和演示：

- `start-local.sh` / `stop-local.sh`：本地开发模式一键启动/停止（Flask + Vue 开发服务器）
- `start-local.bat` / `stop-local.bat`：Windows 本地一键启动/停止
- `start-docker.sh`：Docker 模式一键启动（`--rebuild` 时会重新构建镜像）
- `check-health.sh`：调用 `/api/health` 检查后端服务与数据库状态
- `fix-database.sh` / `fix-db-path.sh`：常见数据库路径、权限问题自动修复
- `quick-fix-db.sh` / `restore-database.sh`：快速重建/从备份恢复 SQLite 数据库文件
- `backend/check_api_config.py`：检查 AI API Key 是否生效、DeepSeek API 连通性
- `backend/kill-port.sh`：在本地开发模式下快速释放被占用的 `5001` 端口

**常见使用场景示例：**
```bash
# 检查健康状态
./check-health.sh

# 修复数据库问题
./fix-database.sh

# 重新启动
./stop-local.sh
./start-local.sh
```

---

## 📁 项目结构

- `backend/` - Flask后端程序
- `frontend/` - Vue.js前端程序
- `start-local.sh` - 本地开发一键启动脚本
- `start-docker.sh` - Docker开发一键启动脚本
- `stop-local.sh` - 停止本地开发服务脚本
- `docker-compose.yml` - Docker编排配置
- `nginx.conf` - Nginx配置文件

---

## 💡 提示

1. **首次启动**：本地模式会自动安装依赖和初始化数据库，可能需要几分钟
2. **开发模式**：本地模式支持热重载，代码修改后自动刷新
3. **生产部署**：Docker模式更接近生产环境，适合测试部署流程
4. **日志查看**：本地模式的日志保存在 `backend.log` 和 `frontend.log`
5. **端口说明**：
   - 本地模式：前端8080，后端5001
   - Docker模式：前端80（Nginx），后端5001

---

## 🆘 仍然无法启动？

1. 检查系统要求：
   - Python 3.9+
   - Node.js 18+
   - Docker & Docker Compose（Docker模式）

2. 查看详细日志：
   - 本地模式：`backend.log` 和 `frontend.log`
   - Docker模式：`docker-compose logs`

3. 检查文档：
   - `README.md` - 项目概述和详细说明
   - `docs/05-使用安装开发说明.md` - 详细安装指南

4. 提交Issue：如果问题仍未解决，请提交Issue并附上错误日志
