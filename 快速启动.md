# 🚀 快速启动指南

本文档提供最简单的一键启动方式，支持：
- 本地开发模式（SQLite + Flask开发服务器）
- Docker开发模式（MySQL + Gunicorn + Nginx）
- 网络部署（让其他学校学生访问）

**需要详细部署说明？** 请查看 `生产环境部署指南.md`

---

## 🎯 一键启动（推荐）

### 方式一：本地开发模式

**macOS/Linux：**
在项目根目录执行：
```bash
./start-local.sh
```

**Windows：**
在项目根目录执行：
```cmd
start-local.bat
```

**就这么简单！** 脚本会自动：
- ✅ 检查Python、Node.js环境
- ✅ 创建虚拟环境并安装依赖
- ✅ 初始化 SQLite 数据库（自动创建，无需配置）
- ✅ 启动后端服务（端口5001）
- ✅ 启动前端服务（端口8080）

**数据库说明：**
- 本地开发使用 **SQLite**（自动创建 `backend/instance/bs_system.db`）
- 无需安装 MySQL，开箱即用

**数据库初始化逻辑：**
- **首次启动（数据库不存在）**：
  1. 自动创建 `backend/instance/bs_system.db` 文件
  2. 创建所有表结构（users、roles、system_settings 等）
  3. 初始化默认角色（admin、teacher、student、user）
  4. 创建默认管理员账户（admin / admin123）
  5. 初始化系统设置（maintenance_mode）
- **再次启动（数据库已存在）**：
  1. 直接使用现有数据库文件
  2. 检查表结构，只创建缺失的表
  3. 检查默认数据，只添加缺失的角色和设置
  4. **不会删除或覆盖现有数据**

**访问地址：**
- 前端：http://localhost:8080
- 后端API：http://localhost:5001/api/health

**默认账户：**
- 用户名：`admin`
- 密码：`admin123`

**停止服务：**
- macOS/Linux: `./stop-local.sh`
- Windows: `stop-local.bat` 或关闭命令行窗口

---

### 方式二：Docker开发模式

在项目根目录执行：

```bash
./start-docker.sh
```

**就这么简单！** 脚本会自动：
- ✅ 检查Docker环境
- ✅ 启动所有容器（默认不重建镜像）
- ✅ 自动启动 MySQL 数据库容器
- ✅ 检查服务健康状态

**数据库说明：**
- Docker 部署使用 **MySQL**（自动启动，数据持久化）
- MySQL 数据存储在 Docker volume `db_data` 中
- 本地开发使用 SQLite，Docker 使用 MySQL，数据独立不互通

**数据库初始化逻辑：**
- **首次启动（数据库不存在）**：
  1. MySQL 容器自动创建数据库 `bs_system`
  2. 后端容器等待 MySQL 就绪后，自动运行 `init_database.py`
  3. 创建所有表结构（users、roles、system_settings 等）
  4. 初始化默认角色（admin、teacher、student、user）
  5. 创建默认管理员账户（admin / admin123）
  6. 初始化系统设置（maintenance_mode）
- **再次启动（数据库已存在）**：
  1. 直接使用现有数据库（数据持久化在 Docker volume 中）
  2. 检查表结构，只创建缺失的表
  3. 检查默认数据，只添加缺失的角色和设置
  4. **不会删除或覆盖现有数据**

如需强制重新构建镜像（前端/后端依赖有变化时）：

```bash
./start-docker.sh --rebuild
```

**访问地址：**
- 前端：http://localhost（通过Nginx）
- 后端API：http://localhost:5001/api/health

**默认账户：**
- 用户名：`admin`
- 密码：`admin123`

**停止服务：**
```bash
docker-compose down
```

---

### 方式三：网络部署（让其他学校学生访问）

**网络部署：**
请查看 `生产环境部署指南.md` 了解详细的网络部署步骤。

**详细部署指南：**
请查看 `生产环境部署指南.md` 中的"网络部署"章节，包含：
- 局域网部署步骤
- 公网部署步骤
- 安全配置建议
- 常见问题排查

**访问地址：**
- 局域网部署：`http://YOUR_SERVER_IP`
- 公网部署：`http://yourdomain.com`

**默认账户：**
- 用户名：`admin`
- 密码：`admin123`
- ⚠️ **首次登录后请立即修改密码！**

---

## 📚 更多文档

- **`生产环境部署指南.md`** - 完整部署文档（Docker部署、网络部署、传统部署）
- **`README.md`** - 项目总览和技术栈说明
- **`docs/`** - 详细设计文档

---

## 🔧 配置说明

### AI功能配置（可选）

如需使用AI健康分析功能，在项目根目录创建 `.env` 文件：

```bash
# DeepSeek API
DEEPSEEK_API_KEY=your-api-key-here
AI_PROVIDER=deepseek

# 或使用通义千问
QWEN_API_KEY=your-api-key-here
AI_PROVIDER=qwen
```

**配置后重启服务：**
- 本地模式：`./stop-local.sh && ./start-local.sh`
- Docker模式：`docker-compose restart backend`

---

## 🔍 故障排查

### 0. 后端显示unhealthy

如果启动后后端显示unhealthy，检查健康状态：

```bash
# 检查后端健康状态
curl http://localhost:5001/api/health

# 查看后端日志
docker-compose logs backend | tail -50
```

**常见原因和解决方法：**

1. **数据库连接失败**
   ```bash
   # 检查 MySQL 服务是否运行
   # macOS/Linux
   sudo systemctl status mysql
   # 或
   brew services list | grep mysql
   
   # 测试 MySQL 连接
   mysql -u bs_user -p bs_system
   
   # 如果连接失败，检查 .env 文件中的数据库配置
   # 重新初始化数据库
   cd backend
   source venv/bin/activate
   python init_database.py
   ```

3. **依赖包缺失**
   ```bash
   cd backend
   source venv/bin/activate
   pip install -r requirements.txt
   ```

4. **查看详细错误信息**
   ```bash
   # 查看后端日志的最后50行
   tail -n 50 backend.log
   
   # 实时查看日志
   tail -f backend.log
   
   # 手动测试健康接口（查看详细错误）
   curl http://localhost:5001/api/health | python3 -m json.tool
   ```

5. **如果数据库连接问题**
   ```bash
   # 检查 MySQL 服务状态
   sudo systemctl status mysql
   
   # 重启 MySQL 服务
   sudo systemctl restart mysql
   
   # 检查数据库是否存在
   mysql -u root -p -e "SHOW DATABASES LIKE 'bs_system';"
   
   # 如果数据库不存在，创建数据库
   mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS bs_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
   
   # 重新初始化
   cd backend
   source venv/bin/activate
   python init_database.py
   ```

**常见错误信息解读：**

- `unhealthy: no such table` - 数据库表不存在，需要重新初始化
- `Can't connect to MySQL server` - MySQL 服务未启动或连接配置错误
- `Access denied for user` - 数据库用户名或密码错误
- `Unknown database` - 数据库不存在，需要先创建数据库
- `ModuleNotFoundError` - 缺少Python依赖包，运行 `pip install -r requirements.txt`

**快速修复（推荐）：**
```bash
# 运行自动修复脚本
# Docker 模式：docker exec bs-system-backend python init_database.py
# 本地模式：cd backend && python init_database.py
```

---

### 1. 端口被占用

**本地模式：**
```bash
# macOS/Linux - 释放端口5001
lsof -ti:5001 | xargs kill -9

# macOS/Linux - 释放端口8080
lsof -ti:8080 | xargs kill -9

# Windows - 查找占用5001端口的进程
netstat -ano | findstr :5001
# 然后杀死进程（替换<PID>为实际PID）
taskkill /PID <PID> /F
```

**Docker模式：**
```bash
# 释放端口80
lsof -ti:80 | xargs kill -9

# 或修改docker-compose.yml中的端口映射
```

---

### 2. 后端启动失败

**检查后端日志：**
```bash
# 本地模式
tail -f backend.log

# Docker模式
docker-compose logs -f backend
```

**常见问题：**

- **数据库错误**：检查 MySQL 服务是否运行，确认 `.env` 文件中的数据库配置正确，然后重新运行 `./start-local.sh`
- **依赖缺失**：进入 `backend` 目录，运行 `pip install -r requirements.txt`
- **Python版本不对**：确保使用Python 3.9+

**手动初始化数据库：**
```bash
cd backend
source venv/bin/activate  # macOS/Linux
python init_database.py
```

---

### 3. 前端启动失败

**检查前端日志：**
```bash
# 本地模式
tail -f frontend.log

# Docker模式
docker-compose logs -f nginx
```

**常见问题：**

- **npm install 失败**：使用国内镜像
  ```bash
  npm config set registry https://registry.npmmirror.com
  npm install
  ```
- **node_modules损坏**：删除后重新安装
  ```bash
  cd frontend
  rm -rf node_modules package-lock.json
  npm install
  ```
- **端口8080被占用**：参考上面的端口释放方法

---

### 4. Docker相关问题

**Docker未启动：**
```bash
# macOS - 打开Docker Desktop应用
# Linux - 启动Docker服务
sudo systemctl start docker
```

**容器启动失败：**
```bash
# 查看所有容器状态
docker-compose ps

# 查看详细日志
docker-compose logs

# 重新构建
docker-compose up -d --build
```

**前端代码更新后不生效（Docker模式）：**
```bash
cd frontend
npm run build
cd ..
docker-compose restart nginx
# 浏览器按 Cmd/Ctrl+Shift+R 硬刷新
```

---

### 5. 数据库问题

**SQLite数据库问题（本地开发）：**
```bash
# 本地开发使用 SQLite，数据库文件自动创建在 backend/instance/bs_system.db
# 如果数据库损坏，删除后重新初始化：
rm backend/instance/bs_system.db
cd backend
python init_database.py
```

**Docker模式数据库（MySQL）：**
- Docker 模式自动使用 MySQL，无需配置
- MySQL 数据存储在 Docker volume 中
- 如需修改 MySQL 配置，编辑 `docker-compose.yml`

---

### 6. 网络连接问题

**前端无法连接后端：**

- **本地模式**：检查 `frontend/vue.config.js` 中的代理配置
- **Docker模式**：检查 `docker-compose.yml` 和 `nginx.conf` 配置

**API请求返回401错误：**
- 清除浏览器缓存和localStorage
- 重新登录

**API请求超时：**
- 检查后端是否正常运行
- 检查防火墙设置
- 增加超时时间（在 `frontend/src/api/request.js` 中）

---

### 7. 权限问题（macOS/Linux）

**脚本无法执行：**
```bash
chmod +x start-local.sh start-docker.sh stop-local.sh
```

**虚拟环境权限错误：**
```bash
cd backend
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

## 📋 手动启动（高级用户）

如果一键启动脚本无法使用，可以手动启动：

### 本地开发模式手动启动

**终端1 - 启动后端：**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
pip install -r requirements.txt
python init_database.py
python run.py
```

**终端2 - 启动前端：**
```bash
cd frontend
npm install
npm run serve
```

### Docker模式手动启动

```bash
docker-compose up -d --build
docker-compose logs -f
```

---

## 🛠️ 工具脚本

项目在根目录提供了一组启动 / 诊断 / 修复脚本，方便开发和演示：

- `start-local.sh` / `stop-local.sh`：本地开发模式一键启动/停止（Flask + Vue 开发服务器）
- `start-local.bat` / `stop-local.bat`：Windows 本地一键启动/停止
- `start-docker.sh`：Docker 模式一键启动（`--rebuild` 时会重新构建镜像）
- `backend/check_api_config.py`：检查 AI API Key 是否生效、DeepSeek API 连通性

**常见使用场景示例：**
```bash
# 检查健康状态
curl http://localhost:5001/api/health

# 重新启动
./stop-local.sh
./start-local.sh
```

---

## 📁 项目结构

- `backend/` - Flask后端程序
- `frontend/` - Vue.js前端程序
- `start-local.sh` - 本地开发一键启动脚本
- `start-docker.sh` - Docker开发一键启动脚本
- `stop-local.sh` - 停止本地开发服务脚本
- `docker-compose.yml` - Docker编排配置
- `nginx.conf` - Nginx配置文件

---

## 💡 提示

1. **首次启动**：本地模式会自动安装依赖和初始化数据库，可能需要几分钟
2. **开发模式**：本地模式支持热重载，代码修改后自动刷新
3. **生产部署**：Docker模式更接近生产环境，适合测试部署流程
4. **日志查看**：本地模式的日志保存在 `backend.log` 和 `frontend.log`
5. **端口说明**：
   - 本地模式：前端8080，后端5001
   - Docker模式：前端80（Nginx），后端5001

---

## 🆘 仍然无法启动？

1. 检查系统要求：
   - Python 3.9+
   - Node.js 18+
   - Docker & Docker Compose（Docker模式）

2. 查看详细日志：
   - 本地模式：`backend.log` 和 `frontend.log`
   - Docker模式：`docker-compose logs`

3. 检查文档：
   - `README.md` - 项目概述和详细说明
   - `生产环境部署指南.md` - 完整部署指南（Docker、网络部署、传统部署）
   - `docs/05-使用安装开发说明.md` - 详细安装指南

4. 提交Issue：如果问题仍未解决，请提交Issue并附上错误日志
