# 🚀 快速启动指南

本文档旨在帮助您快速启动并运行学生健康管理系统。系统支持本地开发模式启动和 Docker 容器化启动。

---

## 🛠️ 方式一：Docker 启动（推荐，最简单）

如果您安装了 Docker 和 Docker Compose，可以使用此方式一键启动整个环境。

### 1. 启动服务
在项目根目录下执行：
```bash
docker-compose up -d
```

### 2. 访问系统
- **前端地址**：[http://localhost](http://localhost) 或 [http://localhost:80](http://localhost:80)（通过 Nginx 反向代理）
- **后端 API**：[http://localhost:5001/api/health](http://localhost:5001/api/health)（可直接访问，或通过前端代理 `/api`）
- **默认管理员账户**：
  - 用户名：`admin`
  - 密码：`admin123`

> 💡 **提示**：Docker 模式下，前端通过 Nginx 在 80 端口提供服务，API 请求会自动代理到后端 5001 端口。

### 3. 配置 AI API Key（可选）

如需使用 AI 功能，请在项目根目录的 `.env` 文件中配置：
```bash
DEEPSEEK_API_KEY=your-api-key-here
AI_PROVIDER=deepseek
```

配置后需要重启后端服务：
```bash
docker-compose restart backend
```

> ⚠️ 如果 DeepSeek 返回 “Insufficient Balance” 会自动切到本地模拟模式，请在 DeepSeek 控制台充值或改用 Qwen：  
> ```
> QWEN_API_KEY=your-qwen-key
> AI_PROVIDER=qwen
> ```

**前端更新或 AI 按钮无日志时（Docker 模式）**  
```bash
cd frontend
npm install
npm run build        # 生成最新 dist
cd ..
docker-compose restart nginx   # 让 Nginx 重新加载前端
# 浏览器 Cmd/Ctrl+Shift+R 硬刷新
```

### 4. 常用命令
```bash
# 查看所有服务日志
docker-compose logs -f

# 查看后端日志
docker-compose logs -f backend

# 查看前端（Nginx）日志
docker-compose logs -f nginx

# 停止服务
docker-compose down

# 重新构建并启动
docker-compose up -d --build

# 重启单个服务（如修改了 .env 配置）
docker-compose restart backend
```

---

## 💻 方式二：本地开发模式启动

### 步骤 1：启动后端服务

1. **进入后端目录**：
   ```bash
   cd backend
   ```

2. **创建并激活虚拟环境**（可选但推荐）：
   ```bash
   python -m venv venv
   source venv/bin/activate  # macOS/Linux
   # venv\Scripts\activate     # Windows
   ```

3. **安装依赖**：
   ```bash
   pip install -r requirements.txt
   ```

4. **初始化数据库**（仅首次运行需要）：
   ```bash
   python init_database.py
   ```
   ✅ 看到 `✅ 数据库初始化完成！` 说明成功。系统默认使用 SQLite 数据库，文件位于 `backend/instance/bs_system.db`。

5. **运行后端**：
   ```bash
   python run.py
   ```
   ✅ 看到 `Running on http://127.0.0.1:5001` 说明启动成功。

---

### 步骤 2：启动前端服务

1. **新开一个终端，进入前端目录**：
   ```bash
   cd frontend
   ```

2. **安装依赖**：
   ```bash
   npm install
   ```

3. **启动开发服务器**：
   ```bash
   npm run serve
   ```
   ✅ 看到 `App running at: http://localhost:8080/` 说明启动成功。

---

### 步骤 3：浏览器访问

打开浏览器访问：[http://localhost:8080](http://localhost:8080)

> ⚠️ **注意**：本地开发模式下，前端运行在 8080 端口，不是 80 端口。前端会自动将 `/api` 请求代理到后端 5001 端口。

- **默认登录账户**：
  - 用户名：`admin`
  - 密码：`admin123`

---

## 📝 核心配置说明

- **数据库**：默认使用 SQLite。如需使用 MySQL，请在项目根目录的 `.env` 文件中配置 `DB_TYPE=mysql` 并填写相关信息。
- **端口配置**：
  - **Docker 模式**：
    - 前端：`80`（通过 Nginx）
    - 后端：`5001`（可直接访问）
  - **本地开发模式**：
    - 前端：`8080`（Vue 开发服务器）
    - 后端：`5001`（Flask 开发服务器）
    - 前端会自动将 `/api` 请求代理到后端
- **AI 功能**：如需开启 AI 报告分析，请在项目根目录的 `.env` 文件中配置：
  ```bash
  DEEPSEEK_API_KEY=your-api-key-here
  # 或
  QWEN_API_KEY=your-api-key-here
  AI_PROVIDER=deepseek  # 或 qwen
  ```
  ⚠️ **重要**：修改 `.env` 文件后需要重启服务才能生效：
  - Docker 模式：`docker-compose restart backend`
  - 本地开发模式：停止并重新运行 `python run.py`

---

## 🔍 常见问题排除

1. **npm install 失败**：请尝试使用国内镜像：`npm config set registry https://registry.npmmirror.com`。
2. **端口被占用**：
   - macOS: `lsof -ti:5001 | xargs kill -9`
   - Windows: `netstat -ano | findstr :5001` 找到 PID 后 `taskkill /PID <PID> /F`。
3. **数据库报错**：如果表结构有重大更新，可以删除 `backend/instance/bs_system.db` 后重新运行 `python init_database.py`。

---

## 📁 项目结构简述

- `backend/`: Flask 后端程序
- `frontend/`: Vue.js 前端程序
- `docker-compose.yml`: Docker 定义文件
- `nginx.conf`: Nginx 配置文件
- `docs/`: 设计与说明文档
