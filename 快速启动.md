# 快速启动指南 - 查看前端效果

## 🚀 三步启动系统

### 步骤1：初始化数据库（首次运行需要）

**打开终端1，执行：**
```bash
cd "/Users/shuirimin/Desktop/BS系统/backend"

# 初始化数据库（创建表结构和管理员账户）
python init_database.py
```

✅ 看到 `✅ 数据库初始化完成！` 说明初始化成功

**注意**：系统默认使用SQLite数据库，无需安装或启动MySQL服务。数据库文件保存在 `backend/bs_system.db`

---

### 步骤2：启动后端服务（必须！）

**继续在终端1，执行：**
```bash
# 启动后端服务
python run.py
```

✅ 看到 `Running on http://127.0.0.1:5001` 说明后端启动成功

---

### 步骤3：启动前端

**打开终端2（新开一个终端窗口），执行：**
```bash
cd "/Users/shuirimin/Desktop/BS系统/frontend"

# 首次运行需要安装依赖
npm install

# 启动前端开发服务器
npm run serve
```

✅ 看到 `App running at: http://localhost:8080/` 说明前端启动成功

---

### 步骤4：在浏览器中打开

打开浏览器，访问：
```
http://localhost:8080
```

**默认登录账户：**
- 用户名：`admin`
- 密码：`admin123`

---

## 📝 重要说明

### 数据库配置

- **默认使用SQLite**：无需安装或启动MySQL服务
- **数据库文件位置**：`backend/bs_system.db`
- **初始化脚本**：`python init_database.py`（一次性完成表创建和管理员账户创建）

### 端口说明

- **后端服务**：`http://localhost:5001`（使用5001避免与macOS AirPlay冲突）
- **前端服务**：`http://localhost:8080`
- **API代理**：前端已配置代理，自动转发到后端5001端口

---

## 🔍 故障排除

### npm install 很慢或失败？

```bash
# 使用国内镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

### 端口被占用？

```bash
# macOS/Linux
lsof -ti:8080 | xargs kill -9

# Windows
netstat -ano | findstr :8080
taskkill /PID <PID> /F
```

### 前端页面空白或报错？

1. 按 `F12` 打开浏览器开发者工具
2. 查看 Console 标签的错误信息
3. 查看 Network 标签，检查API请求是否成功

### 登录失败？

- 确认后端服务正在运行（终端1，端口5001）
- 确认已执行 `python init_database.py` 初始化数据库
- 确认数据库文件 `backend/bs_system.db` 已创建
- 检查浏览器控制台是否有错误信息
- 检查后端终端是否有错误日志

### 数据库连接错误？

- 系统默认使用SQLite，无需MySQL服务
- 如果看到MySQL连接错误，检查是否有设置 `DB_TYPE=mysql` 环境变量
- 删除 `.env` 文件中的 `DB_TYPE=mysql`，重新运行即可使用SQLite

---

## 📱 界面预览

启动成功后，你将看到：

1. **登录页面** - 输入用户名密码登录
2. **主界面** - 包含：
   - 左侧导航菜单
   - 顶部用户信息
   - 主要内容区域

3. **功能模块**：
   - 📊 仪表盘 - 系统统计信息
   - 👥 用户管理 - 用户列表和管理
   - 📝 业务数据 - 业务数据管理

---

## 💡 提示

- 前端支持热重载，修改代码后浏览器会自动刷新
- 两个终端窗口都要保持运行状态（后端和前端）
- **首次运行**：先初始化数据库，再启动后端，最后启动前端
- **后续运行**：直接启动后端和前端即可（数据库已初始化）
- 使用 `Ctrl+C` 可以停止服务
- 默认账户：`admin` / `admin123`

## 🔄 完整启动流程（首次运行）

```bash
# 终端1：初始化数据库（仅首次需要）
cd "/Users/shuirimin/Desktop/BS系统/backend"
python init_database.py

# 终端1：启动后端
python run.py

# 终端2：启动前端
cd "/Users/shuirimin/Desktop/BS系统/frontend"
npm install  # 首次需要
npm run serve

# 浏览器：访问 http://localhost:8080
# 登录：admin / admin123
```

## 🔄 日常启动流程（已初始化后）

```bash
# 终端1：启动后端
cd "/Users/shuirimin/Desktop/BS系统/backend"
python run.py

# 终端2：启动前端
cd "/Users/shuirimin/Desktop/BS系统/frontend"
npm run serve

# 浏览器：访问 http://localhost:8080
```

